<!-- sidebar-right.anchor.handlebars -->
<nav class=" top-32 sidebar-course-right h-fit relative lg:sticky top-0" id="sidebar-course-anchor">

<!-- Branch (version) selection menu. Only visible to 'teachers' team -->

{{#if_equal user.team.slug 'teachers'}}
  <h5 class="m-0 pt-4">Ainekursuse versioonid:</h5>
  <ul class="pl-2 pb-4 mt-4 space-y-1 text-sm text-gray-700 dark:text-gray-200">
  <!-- Radio buttons -->
    {{#each ../branches }}
      <li>
        <div class="flex items-center p-2 rounded hover:bg-gray-100 dark:hover:bg-gray-600">
          <input {{setDefaultChecked this ../../refBranch ../../selectedVersion ../../branches }} id="default-radio-{{@index}}" type="radio" value="{{this}}" name="default-radio" class="form-radio"  onclick="handleRadioClick('{{this}}')" >
          <label for="default-radio-{{@index}}" class="w-full ml-2 text-sm font-medium" onclick="handleRadioClick('{{this}}')" > {{this }}</label>
        </div>
      </li>
    {{/each }}
  </ul>

  <!-- Hidden forms to send req.body values -->
  <form id="my-form" style="display:none;" method="POST" action="/save-selected-version">
  <input type="hidden" name="selectedVersion" id="selectedVersion" >
  <input type="hidden" name="currentPath" id="currentPath" value="/course/{{../path.courseSlug}}/about/" >
  <input type="hidden" name="courseSlug" id="courseSlug" value="{{../path.courseSlug}}" >
  </form>

{{/if_equal}}

<!-- Table of contents -->

  <!-- sidebar sticky behavior beginning from lg size -->
  <div>
    <h5 class="m-0 pt-4">Lehekülje teemad:</h5>
    <div class="pl-2 mt-4 lg:max-h-screen drop-shadow-2xl w-full text-sm">
      {{{ ToC }}}
    </div>
  </div>
  
</nav>


